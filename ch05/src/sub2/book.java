package sub2;

import java.security.DomainCombiner;

/*
<속성>
- title (String) : 책 제목
- author (String) : 저자 이름
- isbn (String) : ISBN 번호
- availableCopies (int) : 이용 가능한 복사본 수

<생산자>
- 모든 인스턴스 변수를 초기화하는 생성자

<메서드>
borrowbook()
 - 이용 가능한 복사본이 있으면 availableCopies를 1 감소시키고 true를 반환
 - 이용 가능한 복사본이 없으면 false를 반환

returnBook()
- availableCopies를 1증가

Show()
- 모든 멤버 변수 값을 출력

<Getter, Setter>
- 모든 속성에 대한 Getter, Setter 정의

*/
public class Book {
	
	// 속성
	private String title;
	private String author;
	private String isbn;
	private int availableCopies;
	

    // 생산자
	public Book(String title, String isbn, String isbn, int availableCopies) {
		this.title = title;
		this.author = author;
		this.isbn = title;
		this.availableCopies = availableCopies;
		
		public boolean borrowBook() {
			
			
			if (availableCopies > 0) {
				availableCopies
				
			}
		}
		
		public void
	}
	
	
			


}
